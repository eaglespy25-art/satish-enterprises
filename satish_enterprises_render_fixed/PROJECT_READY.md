# Satish Enterprises — GitHub Ready

This folder is prepared as the repository root for the Render Node.js deployment.

Important files:
- `src/server.js` — Node.js/Express API
- `package.json` — dependencies
- `render.yaml` — Render service configuration
- `.gitignore` — prevents secrets/node_modules from being committed
- `RENDER_DEPLOY.md` — deployment steps

Do not commit `.env` or real API/payment/database credentials.
